export enum TAdminStatus {
  active = 'active',
  inactive = 'inactive',
  // none = 'none',
}

export enum TProviderApprovalStatus {
  accept = 'accept',
  reject = 'reject',
  pending = 'pending',
  requested = 'requested'
}
